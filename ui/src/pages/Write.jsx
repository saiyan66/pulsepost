export default function Write({ navigate }) {
  return (
    <div>
      <div style={{
        padding: '20px 28px 16px',
        borderBottom: '1px solid var(--border)',
      }}>
        <h1 style={{
          fontFamily: 'var(--serif)',
          fontSize: 22,
          fontWeight: 400,
          color: 'var(--ink)',
        }}>
          New Post
        </h1>
      </div>
      <div style={{
        padding: '40px 28px',
        fontFamily: 'var(--mono)',
        fontSize: 12,
        color: 'var(--text3)',
      }}>
        Write form coming soon.
      </div>
    </div>
  )
}